# interface
library("shiny")
library("rsconnect")

# knits up a shiny object
# shinyApp(ui = ui, server = server)  

# to deploy to a shiny server
# put everything into an app format:

rsconnect::deployApp()

# available at: https://christinale.shinyapps.io/app-1/
# (Google Account Login)




